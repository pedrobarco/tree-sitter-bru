meta {
# <- keyword
  name: Send Bulk SMS
  # <- type
  #     ^ string
}

get {
# <- keyword
  url: https://api.textlocal.in/send
  # <- type
  #    ^ string
  body: json
  # <- type
  #     ^ string
  auth: bearer
  # <- type
  #     ^ string
}

body {
  This is a test message
}
